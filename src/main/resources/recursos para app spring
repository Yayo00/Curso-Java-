Para crear un app web con mysql es importante instalar las dependencias inciales en el proyecto:
1. Spring web
2. h2 databse
3. JDBC spring

war
ver versión estable de maven

En caso de matar proceso 8080
//Ve los puertos activos
netstat -ano -p tcp

//Busca el puerto que se desea
netstat -ano | findstr :PORT_NUMBER

//Mata el proceso del puerto
taskkill /PID puerto_escuchando /F
